# Calling a function - name followed by parantheses with arguments inside
sqrt(9)
abs(3)

#Autocompleting functions in the editor is a useful IDE feature

#str() = structure
str(3)
str("Santa Barbara")

#Another feature: syntax highlighting

#functions can take multiple arguments
pi
round(pi, 0)
round(pi, 5)

n_digits <- 2
round(pi, n_digits)

#functions are a great example of encapsulation, replacing potentially complex
#logic with a very simple name. That makes our ocde far more readable and
#expressive

?sqrt

